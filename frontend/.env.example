# Base URL of the SmartTask backend API.
# Leave unset in development (the Vite dev server proxies /api to localhost:5000).
# In production, set this to your deployed backend, including the /api path:
#   VITE_API_URL=https://your-backend-host.com/api
VITE_API_URL=
